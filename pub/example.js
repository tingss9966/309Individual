"use strict";

const temp = new ScheduleGenerator()
//To enable the developer to add the adding schedule form
temp.init()
let date = new Date()

//The developer may also use this function to create a single schedule
temp.makeSchedule(10,total_month[date.getMonth()], date.getDate(), date.getFullYear(),"example")
changeColor('red')
